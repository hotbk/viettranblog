package com.example.blog.post;

/** File types accepted as post attachments — see {@code PostAttachmentService}. */
public enum AttachmentType {
    PDF,
    DOC,
    DOCX,
    TXT,
    MD,
    SH,
    SQL,
    ZIP
}
