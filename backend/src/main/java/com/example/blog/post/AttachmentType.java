package com.example.blog.post;

/** The 6 file types this MVP accepts as post attachments — see {@code PostAttachmentService}. */
public enum AttachmentType {
    PDF,
    DOC,
    DOCX,
    TXT,
    MD,
    ZIP
}
