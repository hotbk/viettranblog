package com.example.blog.book;

/** Which anchor fields on {@link BookHighlight} are populated — must match the book's {@link BookFileType}. */
public enum HighlightAnchorType {
    TXT_OFFSET,
    PDF_RECTS
}
